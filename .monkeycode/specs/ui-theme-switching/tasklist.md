# 需求实施计划

- [x] 1. 建立主题偏好与启动恢复
  - [x] 1.1 实现主题类型、安全存储和根元素应用模块
    - 定义 `parchment` 与 `minimal` 两个稳定主题标识
    - 在 React 首次渲染前恢复主题并为未知值提供默认主题
    - 对应需求 1.1–1.5、3.1–3.4，设计正确性属性 1–2
  - [x] 1.2 编写主题偏好单元测试
    - 覆盖合法值、未知值、读取异常、写入异常和根元素属性
    - 对应需求 3.1–3.4，设计正确性属性 1–2

- [x] 2. 实现设置页主题选择与极简黑白视觉
  - [x] 2.1 实现可访问主题选择器并接入应用状态
    - 在设置页展示两张主题卡片、视觉样本和当前选中状态
    - 支持鼠标与键盘即时切换，并保持当前业务工作区状态
    - 对应需求 2.1–2.5、5.1–5.2，设计正确性属性 2–3
  - [x] 2.2 将全局颜色与字体迁移到语义化主题变量
    - 保留现有羊皮卷视觉并新增极简黑白颜色、字体、边框和状态令牌
    - 覆盖应用外壳、全部工作区、预览、反馈和确认对话框
    - 对应需求 1.1–1.5、5.2–5.5
  - [x] 2.3 编写主题选择和应用外壳组件测试
    - 覆盖主题卡渲染、即时切换、启动恢复、选中语义和状态保持
    - 对应需求 2.1–2.5、3.2、5.1，设计正确性属性 2–3

- [x] 3. 扩展主题备份与恢复契约
  - [x] 3.1 扩展前端和 Rust 的 `BackupPreferences`
    - 导出当前主题，恢复后同步根元素与本机偏好
    - 为缺少主题字段的历史备份提供 `parchment` 默认值
    - 拒绝包含未知主题标识的备份
    - 对应需求 4.1–4.4，设计正确性属性 4–5
  - [x] 3.2 编写备份主题往返与兼容性测试
    - 覆盖两个合法主题、未知主题、历史备份默认值和前端恢复应用
    - 对应需求 4.1–4.4，设计正确性属性 1、4–5

- [x] 4. 检查点 - 确保所有测试通过
  - [x] 4.1 执行前端与 Rust 质量门禁
    - 运行前端测试、类型检查、生产构建、Rust 格式检查、测试和严格 Clippy
    - 确保所有测试通过,如有疑问请询问用户
