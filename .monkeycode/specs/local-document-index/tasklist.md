# 需求实施计划

- [x] 1. 初始化 Windows 桌面工程和类型化边界
  - [x] 1.1 创建 React、TypeScript、Vite 与 Tauri 2 工程结构
    - 在 `document-index/` 建立前端、Rust core、Tauri 主窗口和 Windows NSIS 配置
    - 配置 pnpm、TypeScript strict、Vitest、Cargo 和开发构建脚本
    - 对应需求 R1.1、R9.1–R9.3、设计 Technology Stack
  - [x] 1.2 定义领域模型、DTO、错误码和 Tauri command 边界
    - 定义索引源、文档、主题、扫描运行、搜索条件、归组建议和扩展名规则类型
    - 建立 React command client 与 Rust command 注册机制
    - 对应需求 R1–R10、设计 Components and Interfaces
  - [x] 1.3 配置最小 Tauri capability 和 Windows 窗口外壳
    - 仅向主窗口开放目录选择和受控 command 权限
    - 配置单实例、窗口恢复和应用数据目录
    - 对应需求 R1.1、R9.1–R9.3、设计 Security and Privacy
  - [x] 1.4 配置前后端测试基础设施
    - 配置 React Testing Library、fast-check、Rust proptest 和临时目录 fixture
    - 对应设计 Test Strategy

- [x] 2. 建立 SQLite 元数据索引和仓储
  - [x] 2.1 实现数据库连接、迁移和事务执行器
    - 创建 `index_sources`、`documents`、`topics`、`grouping_suggestions`、`manual_grouping_rules`、`scan_runs`、`scan_errors` 和 `extension_rules`
    - 启用 WAL、外键、必要索引和迁移日志
    - 对应需求 R1.2、R7.4、R9.1、设计 Data Models
  - [x] 2.2 实现元数据、主题和扫描仓储
    - 实现分批 upsert、缺失标记、主题聚合、扫描进度和错误记录
    - 保证文档、主题时间标记和搜索索引在事务中一致更新
    - 对应需求 R2.2、R5.1–R5.8、R7.1–R7.5、设计正确性属性 P1、P3、P4、P8、P9
  - [x] 2.3 建立元数据 FTS5 搜索索引
    - 仅索引主题名、文件名、规范化名称和完整路径
    - 实现迁移时重建和文档变更时增量同步
    - 对应需求 R6.1–R6.7、R9.2、设计正确性属性 P6
  - [x] 2.4 编写 SQLite 仓储集成测试
    - 验证迁移、事务回滚、FTS5 同步、主题聚合和分页稳定性
    - 对应设计正确性属性 P1、P3–P6、P8

- [x] 3. 实现索引源、文档发现和扫描任务
  - [x] 3.1 实现索引源校验与管理服务
    - 支持磁盘或目录添加、路径规范化、重叠检测、暂停和可访问状态
    - 实现默认扩展名和自定义扩展名白名单校验
    - 对应需求 R1.1–R1.5、R2.4、R2.6–R2.7、设计正确性属性 P7
  - [x] 3.2 实现文档名称与版本标记规范化
    - 提取数字版本、日期、修订版、终稿、副本和 copy 标记
    - 输出原始文件名、规范化名称、版本标签和排序键
    - 对应需求 R3.1–R3.5、设计 Name Normalization
  - [x] 3.3 实现可取消、可恢复的后台扫描协调器
    - 递归遍历索引源并仅读取路径、名称、扩展名、大小、创建时间和最后修改时间
    - 分批写入数据库，推送进度，隔离访问错误并保存继续游标
    - 对应需求 R2.1–R2.5、R7.1、R7.3–R7.5、R10.1、R10.3–R10.5、设计正确性属性 P6–P10
  - [x] 3.4 注册索引源和扫描 Tauri commands
    - 实现 `list_sources`、`add_source`、`set_source_enabled`、`start_scan`、`cancel_scan` 和 `get_scan_status`
    - 对应需求 R1、R7.1、R7.4、R10.1
  - [x] 3.5 编写规范化与扫描测试
    - 覆盖版本命名样例、空目录、权限失败、取消恢复和索引源边界
    - 对应设计正确性属性 P6–P10

- [x] 4. 实现智能归组和双时间版本模型
  - [x] 4.1 实现候选阻塞键和归组评分
    - 基于规范化名称、关键词相似度、编辑相似度、版本标记、文件类型和路径主题词评分
    - 高置信度自动归组，中置信度生成待整理建议，低置信度建立独立主题
    - 对应需求 R4.1–R4.4、设计 Grouping Score
  - [x] 4.2 实现主题双时间标记和稳定版本排序
    - 分别计算“最新创建”和“最近修改”文档
    - 支持创建时间、最后修改时间、版本号和文件名排序以及稳定并列规则
    - 对应需求 R5.1–R5.8、设计正确性属性 P3–P5
  - [x] 4.3 实现人工重命名、合并和拆分服务
    - 以事务更新主题归属、人工规则、版本数量和双时间标记
    - 后续扫描保持人工主题名称和文档归属
    - 对应需求 R4.5–R4.6、R8.1–R8.5、设计正确性属性 P1–P4、P8
  - [x] 4.4 注册主题管理 Tauri commands
    - 实现 `rename_topic`、`merge_topics`、`move_documents_to_topic` 和 `list_organize_suggestions`
    - 对应需求 R4、R8
  - [x] 4.5 编写归组与排序 property-based tests
    - 分别验证 P1 单主题归属、P2 人工优先、P3 最新创建、P4 最近修改和 P5 稳定排序
    - 对应需求 R4.1–R4.6、R5.1–R5.8

- [x] 5. 实现搜索、主题详情、文件预览和安全回收
  - [x] 5.1 实现主题检索、筛选、排序和分页服务
    - 匹配主题名、文件名、规范化名称和路径
    - 支持索引源、目录、创建时间和最后修改时间筛选
    - 对应需求 R6.1–R6.5、R10.2、设计正确性属性 P5
  - [x] 5.2 实现主题详情查询
    - 返回全部版本、双时间标记、版本标记、创建时间、修改时间、类型、路径和可用状态
    - 对应需求 R5.1–R5.8、R6.4–R6.5
  - [x] 5.3 实现受控文件打开和 Explorer 定位
    - 根据文档 ID 解析路径，重新校验索引源边界和文件状态后调用 Windows Shell
    - 对应需求 R6.6、设计 Security and Privacy
  - [x] 5.4 注册搜索与文件操作 Tauri commands
    - 实现 `search_topics`、`get_topic_detail`、`open_document` 和 `reveal_document`
    - 对应需求 R6.1–R6.7
  - [x] 5.5 实现内置按需预览适配器
    - 支持 PDF、图片、TXT、Markdown、CSV、JSON、DOCX、XLSX 和 PPTX 只读预览
    - 使用短期预览会话、文件大小限制和安全渲染，正文不进入索引或日志
    - 对应需求 R12.3–R12.6、R12.8–R12.9、R12.12、设计正确性属性 P11–P12
  - [x] 5.6 实现 Windows Preview Handler 适配器
    - 为 DOC、XLS、PPT 等旧格式托管已注册的 `IPreviewHandler`
    - 同步工作区预览区域尺寸，并在切换文件时调用 `Unload`
    - 对应需求 R12.7–R12.9、设计 WindowsPreviewHost
  - [x] 5.7 实现 Windows 回收站服务
    - 对单个或多个文档执行明确确认，将文件移入回收站并记录结果
    - 成功后更新文档状态、主题版本数量和双时间标记，失败时保持索引不变
    - 对应需求 R11.1–R11.7、设计正确性属性 P13–P14
  - [x] 5.8 注册预览与回收站 Tauri commands
    - 实现预览创建、调整尺寸、关闭、文档回收和打开回收站 commands
    - 所有入口按文档 ID 重新校验索引源和文件状态
    - 对应需求 R11、R12.3–R12.12
  - [x] 5.9 编写搜索、预览与文件操作测试
    - 覆盖空查询、组合筛选、时间并列、稳定分页、越界路径和文件缺失
    - 覆盖预览资源释放、正文零持久化、处理程序缺失、回收成功和回收失败
    - 对应设计正确性属性 P5、P7、P11–P14

- [x] 6. 实现桌面检索与资料整理界面
  - [x] 6.1 实现应用外壳和索引状态栏
    - 创建左侧固定侧边栏与右侧完整工作区，注册搜索、资料库、待整理、索引位置和设置导航
    - 展示扫描进度、文件数、主题数、失败数和最近完成时间
    - 对应需求 R7.4、R10.1、R10.5、R12.1
  - [x] 6.2 实现搜索工作台和主题结果列表
    - 提供搜索框、位置筛选、双时间筛选、排序和分页
    - 在同一工作区建立可拖动的版本列表与预览区域，展示版本数量、最新创建、最近修改和所在位置
    - 对应需求 R5.1–R5.6、R6.1–R6.7、R12.2、R12.10–R12.11
  - [x] 6.3 实现主题详情和版本操作
    - 展示全部版本、双时间标记、原始名称、规范化名称、路径和文件操作
    - 支持四种排序方式、多选、回收站确认并持久化用户默认时间维度
    - 对应需求 R3.5、R5.1–R5.8、R6.5–R6.6、R11
  - [x] 6.4 实现索引源与扩展名设置
    - 使用原生对话框添加目录，支持暂停、刷新、错误查看和自定义扩展名
    - 对应需求 R1、R2.4、R2.6–R2.7、R7.4
  - [x] 6.5 实现待整理和人工主题编辑
    - 展示系统合并建议、归组证据和置信度
    - 支持主题重命名、合并和文档拆分
    - 对应需求 R4.3–R4.5、R8.1–R8.5
  - [x] 6.6 实现文件预览面板
    - 支持只读内容、缩放、页码或工作表切换、折叠、工作区全宽和预览兜底状态
    - 切换文件或关闭面板时释放预览会话
    - 对应需求 R12.3–R12.12
  - [x] 6.7 编写前端组件测试
    - 覆盖检索筛选、空状态、双时间标记、排序切换、扫描状态和人工整理
    - 覆盖两栏布局、分隔线偏好、预览切换、全宽模式和回收站确认
    - 对应需求 R1、R4–R8、R10.1、R11–R12

- [x] 7. 实现增量更新、备份和运行恢复
  - [x] 7.1 实现索引源文件变化监控
    - 合并新增、修改、重命名、移动和移除事件并触发局部更新
    - 监听事件溢出时调度受影响目录复扫
    - 对应需求 R7.2–R7.3、设计 WatchService
  - [x] 7.2 实现启动恢复和不可访问源处理
    - 启动时恢复未完成扫描，校验索引源状态并保留离线磁盘索引
    - 对应需求 R1.4、R7.1、R10.4
  - [x] 7.3 实现索引配置备份与恢复
    - 导出和恢复索引源、主题关系、人工修正、扩展名和排序偏好
    - 恢复后重新校验路径与文件可访问状态
    - 对应需求 R9.4–R9.5
  - [x] 7.4 编写增量更新与恢复集成测试
    - 覆盖文件变化、事件合并、监听溢出、离线源、扫描继续和备份恢复
    - 对应需求 R7、R9.4–R9.5、R10.4

- [x] 8. 检查点 - 确保所有测试通过
  - [x] 8.1 执行前端与 Rust 质量门禁
    - 运行前端测试、类型检查、生产构建、Rust 格式检查、测试和严格 Clippy
    - 确保所有测试通过,如有疑问请询问用户

- [x] 9. 完成 Windows 打包和首版验收自动化
  - [x] 9.1 配置 Windows NSIS 安装包
    - 配置当前用户安装、简体中文界面、开始菜单和桌面快捷方式
    - 对应需求 R9.1–R9.3、设计 Technology Stack
  - [x] 9.2 建立核心流程自动化验收
    - 使用临时目录串联添加索引源、扫描、跨目录归组、双时间排序、搜索和人工修正
    - 验证默认与自定义扩展名只采集元数据
    - 对应需求 R1–R10、设计正确性属性 P1–P10
  - [x] 9.3 建立十万条元数据性能测试
    - 验证基线设备上常用检索在 500 毫秒内返回
    - 对应需求 R10.2、设计 Performance Tests

- [x] 10. 最终检查点 - 确保所有测试通过
  - [x] 10.1 执行完整交付验证
    - 运行全部前端与 Rust 门禁、Tauri desktop feature 编译和补丁检查
    - 确保所有测试通过,如有疑问请询问用户

- [x] 11. 修复首次启动引导与首次扫描可靠性
  - [x] 11.1 实现空来源首次使用引导
    - 应用启动时读取索引源，空来源直接进入索引位置并展示显著的目录选择入口
    - 侧栏以“请添加索引位置”替代无操作指引的等待状态
    - 对应需求 R1.1–R1.2、R7.1、R10.1
  - [x] 11.2 同步来源与扫描启动状态
    - 来源新增、启停和扫描启动后立即同步应用外壳的来源数量、扫描 ID 与索引快照
    - 保留 `add_source` 与 `start_scan` 的独立 command 边界
    - 对应需求 R1、R7.1、R7.4、R10.1
  - [x] 11.3 增加启动首次扫描兜底与回归测试
    - 桌面启动恢复未完成扫描后，自动启动启用、在线且从未扫描的来源
    - 排除未完成运行中的来源并使用 `last_scan_at` 防止重复首次扫描
    - 覆盖空来源引导、来源状态回传、扫描启动回传、首次启动兜底和重复扫描防护
    - 对应需求 R7.1、R10.1、R10.4
