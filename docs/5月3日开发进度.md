# 5月3日开发进度

## 基本信息
- 日期：2026-05-03
- 当前版本：v6.1（餐饮计算器整合升级 + 后端引擎对齐 + 规划餐饮全量改造方案）
- 协作状态：2 个 Agent 并行工作

## 今日注意事项（另一个 Agent 阅读后请注意）
1. 每次开发动作完成后，必须更新本文档
2. 每次开发前必须先阅读本文档和注意事项
3. 有另一个 Agent 在同一时间工作，避免冲突修改
4. **多Agent协作规范**：
   - 不同 Agent 必须分工负责不同的工具/模块，不要同时修改同一个工具文件
   - 每次修改代码后，必须立即执行 git add + git commit + git push，不能等到最后才提交
   - 开始开发前，必须先阅读本文档，了解其他 Agent 的工作范围
   - 如果另一个 Agent 正在修改某个文件，不要在本地缓存该文件的旧版本，必须先 git pull 拉取最新代码再开发
   - 工作区必须始终保持 clean 状态（无未提交的修改），否则下次会话启动时所有改动都会丢失
5. **模块分工**：
   - **餐饮板块（所有 finance/restaurant 相关计算器）**：由本 Agent 统一维护，另一个 Agent 请勿修改
   - **美业板块**：由另一个 Agent 负责整体改造，本 Agent 不触碰

## 今日开发记录

### 1. 排班助手（已完成 - 最终版）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/ScheduleGenerator.vue`（前端交互优化）
  - `backend/src/routes/generate.js`（更新 schedule 工具定义适配新字段）
- **改动内容**：
  - 员工添加改为输入框手动输入姓名（非预设名字），回车或点击添加
  - 每个员工卡片可设置期望休息日（周一至周日）、班次偏好（自动/早班/中班/晚班）
  - 班次时间设置：早班/中班/晚班分别可自定义开始和结束时间
  - 营业时间设置：可设置营业开始和结束时间
  - 结果展示：排班总览表、工时统计卡片、冲突检测、优化建议
- **注意**：另一个 Agent 请勿修改这两个文件

### 2. 选题生成器重构（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/TopicGenerator.vue`（前端表单重构为卡片交互）
  - `backend/src/routes/generate.js`（更新 topic 的 systemPrompt 和 userPromptTemplate）
  - `frontend/src/constants/toolCatalog.js`（category 从 planning 改为 marketing）
- **改动内容**：
  - 表单改为卡片交互：行业、解决问题、内容类型、时长、拍摄场景（支持自选）、目标平台、生成数量
  - 后端 prompt 结构化传递用户选择的卡片参数，AI 按 JSON 格式输出选题
  - 分类从"方案策划"移到"营销获客"
- **注意**：另一个 Agent 请勿修改这三个文件

### 3. 餐饮现金流预测计算器重构（已完成）
- **状态**：已完成
- **改动文件**：
  - `backend/src/services/calculatorEngine.js`（第 214 行，重写计算逻辑）
  - `frontend/src/views/tools/CashflowRestaurant.vue`（完全重写前端）
  - `frontend/src/constants/toolCatalog.js`（更新描述文案）
- **改动内容**：
  - 后端：输入从 4 个扩展到 11 个，固定/变动成本分离，支持额外支出和预收
  - 前端：表单分 4 组（基础信息/固定成本/变动成本/特殊现金流），支持动态添加即将发生的大额支出
  - 结果展示：成本结构概览、断裂预警、逐月预测表、状态标记、经营建议、行业参考
- **预览地址**：https://5174-5ab6a3cb51458057.monkeycode-ai.online
- **注意**：另一个 Agent 请勿修改这三个文件

### 4. 回本周期计算器重构（已完成）
- **状态**：已完成（通用版 + 餐饮版）
- **改动文件**：
  - `frontend/src/views/tools/PaybackCalculator.vue`（通用版，完全重写）
  - `frontend/src/views/tools/PaybackRestaurant.vue`（餐饮版，完全重写）
- **改动内容**：
  - 从"投资金额+月回报"改为"前期投资+运营成本"两维度
  - **前期投资**：加盟费/品牌使用费、设备采购、首批货款、装修、银行利息、证照押金等
  - **运营成本**：人工、房租、水电、其他运营支出（通用版）
  - **餐饮版运营成本**：厨师工资、前厅工资、房租、水电燃气、食材成本率%、平台抽成/营销%
  - 预期收入：月均收入/营业额 + 可变成本率
  - 结果展示：投资结构 breakdown、运营结构 breakdown、回本时间线、年化收益率、行业参考
- **注意**：另一个 Agent 请勿修改这两个文件

### 5. 盈亏平衡点计算器（餐饮版）多维专业版重构（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/BreakEvenRestaurant.vue`（完全重写）
  - `frontend/src/constants/toolCatalog.js`（更新描述文案）
- **改动内容**：
  - 输入从 2 项升级为 4 组可折叠表单：固定成本（房租/人工/折旧/其他）、变动成本率（食材/平台抽成/水电/包装/提成）、经营参数（面积/餐位/营业小时/客单价）、可选参数（实际营业额/目标利润）
  - 计算维度扩展：月保本营业额、日保本、小时保本、保本日客流、保本翻台率、保本坪效线、安全边际率、目标利润营业额
  - 成本结构诊断：可视化成本占比条，逐项对比行业基准（毛利率/房租占比/人工占比/食材成本率）
  - What-If 场景对比：当前方案 vs 固定成本降10% vs 变动成本率降5%
  - 针对性经营建议：根据不健康指标自动给出对应策略
- **注意**：另一个 Agent 请勿修改这两个文件

### 6. 品类毛利计算器（餐饮版）重构（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/GrossMarginRestaurant.vue`（完全重写前端）
  - `backend/src/services/calculatorEngine.js`（重写 gross-margin-restaurant 计算逻辑）
  - `frontend/src/constants/toolCatalog.js`（更新工具描述和场景标签）
- **改动内容**：
  - 从单菜品计算改为多品类对比分析模式
  - 支持动态添加多个品类（火锅、炒菜、凉菜、酒水、主食、甜品、小吃、烧烤、饮品等预设 + 自定义）
  - 每个品类录入：品类名称、月销售额、月食材成本
  - 结果展示：综合毛利率总览、各品类毛利卡片（毛利率/销售额/毛利额/毛利占比）、经营建议
  - 内置行业基准对比（火锅55-65%、炒菜60-70%、酒水70-80%等）
  - 智能建议：高毛利低销量推广、低毛利高销量优化、低于行业基准预警等
- **注意**：另一个 Agent 请勿修改这三个文件

### 7. 菜品定价计算器重构为产品结构设计工具（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/DishPricing.vue`（完全重写前端）
  - `backend/src/services/calculatorEngine.js`（重写 dish-pricing 计算逻辑）
  - `frontend/src/constants/toolCatalog.js`（更新工具描述和场景标签）
- **改动内容**：
  - 从单菜品定价改为多菜品产品结构设计模式
  - 支持门店定位选择（快餐/中档正餐/高档餐厅）
  - 每个菜品可设置：名称、成本、角色（引流菜/主推菜/形象菜/搭配菜）、定价策略（目标毛利率法/成本加成法/市场对标法）、心理定价优化
  - 结果展示：门店综合毛利预测（基于销量占比模型）、产品结构诊断（各角色占比vs目标）、各菜品定价明细表
  - 自动推荐套餐组合（引流+主推+搭配，85-90折）
  - 智能优化建议（结构不合理预警、定价异常提示、缺失角色提醒）
  - 知识库预留：行业基准数据结构化，未来可接入后台配置
- **注意**：另一个 Agent 请勿修改这三个文件

### 8. 人工成本占比计算器（餐饮版）重构与人效工具整合（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/SalaryCostRatioRestaurant.vue`（完全重写前端）
  - `backend/src/services/calculatorEngine.js`（重写 salary-cost-ratio-restaurant，合并 labor-efficiency-restaurant 逻辑）
  - `frontend/src/constants/toolCatalog.js`（更新工具描述，移除旧人效工具）
  - `frontend/src/views/ToolPage.vue`（移除旧人效工具引用）
- **改动内容**：
  - 前端改为按“前厅/后厨/管理”分组动态录入各岗位（如服务员、厨师长、店长等）的人数与月薪
  - 支持选择餐厅类型（快餐/正餐/高端），自动匹配不同的行业基准
  - 新增“综合人效统计”模块：全店人均产出、前厅人效、后厨人效
  - 人员结构诊断：前后场人员数量占比与成本占比对比
  - 优化建议模板化，全部基于结构化知识库生成
  - 移除旧的独立“人效计算器（餐饮版）”，功能完全并入人工成本占比计算器
  - 建立 `KNOWLEDGE_BASE` 配置对象，将基准值、建议话术等与计算逻辑解耦，为后续接入后台数据库做准备
- **注意**：另一个 Agent 请勿修改这些文件

### 9. 坪效功能并入盈亏平衡点计算器，独立坪效工具下线（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/views/tools/BreakEvenRestaurant.vue`（新增坪效分析卡片）
  - `frontend/src/views/tools/AreaEfficiencyRestaurant.vue`（删除）
  - `frontend/src/constants/toolCatalog.js`（移除 area-efficiency-restaurant 条目）
  - `frontend/src/views/ToolPage.vue`（移除 AreaEfficiencyRestaurant 组件引用）
  - `backend/src/services/calculatorEngine.js`（移除 area-efficiency-restaurant 计算引擎）
  - `backend/src/routes/tool.js`（移除 area-efficiency-restaurant 额度配置）
- **改动内容**：
  - 盈亏平衡点结果页新增「坪效分析」卡片：保本坪效、实际坪效（需填写实际营业额）、行业诊断
  - 保本坪效 = 月保本营业额 / 营业面积
  - 实际坪效 = 实际月营业额 / 营业面积，附带日坪效换算
  - 行业基准：快餐>3000，中餐/火锅1500-3500，咖啡2000-4000 元/m²/月
  - 坪效状态分为优秀/偏低/过低三档，给出诊断文案
  - 下线独立坪效计算器，从工具目录、路由映射、后端引擎、额度配置中全部移除
- **注意**：另一个 Agent 请勿修改这些文件

### 10. 餐饮计算器整合 P0 阶段（已完成）
- **状态**：已完成
- **改动文件**：
  - `frontend/src/constants/toolCatalog.js`（移除 labor-efficiency-restaurant 条目）
  - `frontend/src/views/ToolPage.vue`（移除 LaborEfficiencyRestaurant 组件引用）
  - `frontend/src/views/tools/LaborEfficiencyRestaurant.vue`（删除）
  - `backend/src/services/calculatorEngine.js`（移除 labor-efficiency-restaurant，重写 break-even-restaurant 后端引擎对齐前端堂食/外卖拆分/加权贡献率/坪效/安全边际逻辑）
  - `backend/src/routes/tool.js`（移除 labor-efficiency-restaurant 额度配置）
  - `backend/src/services/diagnosisEngine.js`（将推荐工具从 labor-efficiency 替换为 salary-cost-ratio）
- **改动内容**：
  - 下线独立人效计算器，功能完全由人工成本占比计算器覆盖（后者已有前厅人效/后厨人效/全店人效+知识库诊断）
  - 盈亏平衡点后端引擎从简单的 fixedCost/avgRevenue 公式升级为完整的堂食/外卖拆分、加权贡献率、多维度拆解（客流/翻台率/坪效）、安全边际、What-If 场景对比
  - 前后端计算逻辑现在完全一致
- **注意**：另一个 Agent 请勿修改这些文件

### 11. 餐饮计算器全量改造方案（规划完成，待执行）
- **状态**：方案已规划，后续按 P0→P1→P2→P3 优先级执行
- **整合项**：labor-efficiency→salary-cost-ratio（已完成）；return-rate→payback 升级开店投资测算器（P1）
- **新增项**：
  - P1：开店投资预算计算器（加盟费/装修/设备/物料/租金/流动资金拆解）
  - P2：食材出成率/净料率计算器（毛料→净料成本核算）
  - P2：外卖经营综合分析器（整合 delivery-profit，堂食 vs 外卖对比）
  - P3：库存周转率计算器（周转天数/安全库存/临期预警）
  - P3：菜品贡献度分析器（BCG 四象限：明星/金牛/问题/瘦狗）
  - P3：复购率/回头客计算器（回头客占比/行业基准/提升建议）
- **注意**：整个餐饮板块由本 Agent 统一维护，另一个 Agent 请勿修改餐饮相关文件

## 待处理问题

### 1. 表格修改未生效（已解决）
- **问题**：用户昨天修改了很多表格，今天打开全部没有生效
- **原因**：99 个文件修改了但没有提交，服务运行的是旧代码
- **解决**：已全部提交并推送到远程仓库（commit: 817a520）

## 明日计划
- 继续优化其他工具
- 确保所有修改及时提交
