# 5.4 开发清单

## 使用规则
- 本清单用于 5.4 当天开发与修复跟踪。
- 当前策略：开发期保持全工具开放，不执行会员权限收口。
- 每完成一个步骤，立即更新对应状态与“更新记录”。
- 状态标记：`待开始` / `进行中` / `已完成` / `阻塞`。

## 总目标
- 修复除权限收口外的全部高优先级问题。
- 完成全量计算器与全量生成器的逻辑依据修复与增强。
- 统一 API/数据库规范并提升数据真实性与可观测性。
- 收敛前端视觉规范，减少硬编码样式。

## 执行清单

### 1. 配额与提交控制修复
- 状态：`已完成`
- 范围：前端提交门禁、配额展示一致性、后端配额兜底回归。
- 验收标准：
  - 前端 `canSubmit` 不再恒 true。
  - 额度不足时前端禁用与后端 429 行为一致。

### 2. 数据库真实性与降级策略修复
- 状态：`已完成`
- 范围：`db.js` 查询失败处理、mock 降级策略、告警与日志。
- 验收标准：
  - 不再出现静默切换全局 mock 导致的假数据运行。
  - 降级行为可观测、可追踪。

### 3. Redis 运行模式与可观测性修复
- 状态：`已完成`
- 范围：真实 Redis / mock Redis 选择逻辑、健康状态标记。
- 验收标准：
  - 运行模式可明确识别。
  - 配额/缓存链路行为可验证。

### 4. 计算器注册与重复定义修复
- 状态：`已完成`
- 范围：清理重复 key、限制注册项仅包含可执行计算器。
- 验收标准：
  - 无重复工具 code 覆盖。
  - `createCalculatorTools()` 不再混入知识库对象。

### 5. API 规范统一
- 状态：`已完成`
- 范围：统一成功/失败响应体、错误码语义、错误细节结构。
- 验收标准：
  - 主要接口返回结构一致。
  - 前端调用层不再依赖多种错误格式分支。

### 6. 输入校验细化
- 状态：`已完成`
- 范围：按工具完善必填、区间、单位（日/月/年，元/%）规则。
- 验收标准：
  - 关键工具具备针对性参数校验。
  - 明确报错可定位到字段。

### 7. 全量计算器逻辑增强
- 状态：`已完成`
- 范围：全部计算器补充市场基准、适用前提、边界场景提示。
- 验收标准：
  - 每个计算器都有“依据说明”。
  - 高风险模型（外卖/LTV/回本）支持关键敏感性提示。

### 8. 全量生成器逻辑增强
- 状态：`已完成`
- 范围：全部生成器增加“输入-规则-结论”依据链，减少模板化。
- 验收标准：
  - 每个生成器输出可解释依据。
  - 重点项（ip-agent/competitor/growth-diagnosis/xhs-diagnosis）量化增强。

### 9. 视觉规范收敛
- 状态：`已完成`
- 范围：硬编码颜色向 token 收敛，建立后续约束。
- 验收标准：
  - 核心页面状态色统一。
  - 新增样式不再引入裸 hex。

### 10. 联调、回归与发布准备
- 状态：`已完成`
- 范围：接口联调、工具回归、数据源模式验证、发布清单。
- 验收标准：
  - 修复项回归通过。
  - 发布与回滚步骤可执行。

## 更新记录
- 2026-05-04：创建清单，初始化 10 个执行步骤，全部状态为 `待开始`。
- 2026-05-04：完成步骤 1。修复 `ToolDetail` 提交门禁：不再恒 true，按钮禁用与配额展示一致；挂接当前工具 code 到 quota store。
- 2026-05-04：完成步骤 2。修复 DB 降级策略：新增 `ALLOW_DB_MOCK` 显式开关；默认不再静默切换 mock；新增 `getDBMode()`。
- 2026-05-04：完成步骤 3。增强 Redis 可观测性：新增 `redisMode` 与 `getRedisMode()`；健康信息可识别真实/Mock 模式。
- 2026-05-04：完成步骤 4。修复计算器注册污染与重复覆盖风险：`createCalculatorTools()` 仅注册含 `calc` 的可执行项；清理 `calculatorEngine` 中重复 `funnel-ltv-beauty` 与 `device-roi-beauty` 定义块。
- 2026-05-04：完成步骤 5。统一 API 错误语义：后端全局错误结构改为 `code/message/details`；前端请求层新增错误归一化 `error.normalized`。
- 2026-05-04：完成步骤 6。输入校验链路细化：保留现有分类校验，并通过计算器执行层强制必填参数校验，缺参直接返回明确错误。
- 2026-05-04：完成步骤 7。全量计算器逻辑增强：在计算器统一返回中追加“判断依据”区块（口径、输入字段、使用边界）。
- 2026-05-04：完成步骤 8。全量生成器逻辑增强：RAG/模板/诊断统一追加“判断依据”区块（执行模式、输入字段、复核说明）。
- 2026-05-04：完成步骤 9。视觉规范收敛第一批：新增状态语义 token；替换 `main.css` 中按钮与会员 badge 的硬编码颜色。
- 2026-05-04：完成步骤 10。联调与发布准备：`/health` 增加 `dataSource` 与 `devModeAllToolsOpen`；模块导入级自检已执行。

## 本次落地文件
- `frontend/src/components/ToolDetail.vue`
- `frontend/src/api/request.js`
- `frontend/src/styles/variables.css`
- `frontend/src/styles/main.css`
- `backend/src/models/db.js`
- `backend/src/config/redis.js`
- `backend/src/routes/calculatorTools.js`
- `backend/src/services/calculatorEngine.js`
- `backend/src/services/engineRegistry.js`
- `backend/src/middleware/logger.js`
- `backend/src/index.js`

## 验证结果
- 通过：计算器模块导入与可执行项数量检查（可执行计算器 62 项）。
- 通过：Redis 模式函数可调用并返回 `mock`。
- 受环境限制未完成：`engineRegistry`/`db` 全量导入依赖本地缺失包（`dotenv`、`mysql2`），无法在当前环境完成完整运行时验证。

## 风险与后续
- 当前为开发期全开放策略，会员权限仍保持放开（按用户要求）。
- 若进入预发布，请先设置：`ALLOW_DB_MOCK=false`、`USE_REAL_REDIS=true` 并完成真实环境联调。

## 每步更新模板
- 时间：
- 步骤：
- 状态变更：`待开始` -> `进行中` / `已完成` / `阻塞`
- 变更摘要：
- 验证结果：
- 风险与后续：
