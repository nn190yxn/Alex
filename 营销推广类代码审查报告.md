# 营销推广类代码审查报告

**审查范围**：本次新增的 10 个营销推广计算器 + 6 张营销推广数据表  
**审查日期**：2026-05-03  
**审查结论**：共发现 28 个问题，其中严重级 17 个，中等 7 个，轻微 4 个

---

## 一、严重问题（17 个）

### BUG-01 ~ BUG-10：10 个计算器前后端 API 字段完全不匹配

**严重程度**：严重 — 后端计算器引擎完全无法被这 10 个前端工具调用。

所有 10 个计算器的前端组件内部自行计算逻辑（纯前端计算），但 `toolCatalog.js` 中注册的 `category: 'marketing'` 意味着这些工具会走后端 `calculatorEngine.js` 路由，而前后端字段名完全不匹配，后端收到 undefined 全部参数，要么报错要么返回 NaN 结果。

具体对照表：

| # | 工具代码 | 前端发送字段 | 后端期望字段 | 匹配结果 |
|---|---------|------------|------------|---------|
| 1 | channel-cac | channels[].{name, cost, leads, converted} | channels[].{name, cost, leads, visits, deals} | 不匹配：`converted` vs `deals`，缺 `visits` |
| 2 | campaign-roi | {totalCost, days, newVisitors, orders, revenue, grossMargin} | {campaignName, investment, leads, visit, deals, revenue, marginRate} | 全部不匹配 |
| 3 | referral-roi | {oldCustomers, newCustomers, rewardCost, newRevenue, otherCAC} | {totalCustomers, referralCount, referralDeals, rewardPerReferral, newCustomerCac} | 全部不匹配 |
| 4 | conversion-funnel | stages[].{name, count}（动态数组） | {leads, experience, deals, repurchase}（固定4个字段） | 结构完全不同 |
| 5 | retention-rate | {startCustomers, newCustomers, endActive, dormantDays} | {totalCustomers, customers30, customers60, customers90, avgVisitInterval} | 全部不匹配 |
| 6 | marketing-budget | {totalBudget, goal, channels[].{key, enabled, cac}} | {totalBudget, goal, industry} | channels 数据被后端忽略，后端用硬编码百分比 |
| 7 | churn-rate | {startCustomers, churned, avgOrder, freq} | {startCustomers, endCustomers, newCustomers, avgLtv} | 只有 startCustomers 匹配 |
| 8 | ltv-restaurant | {avgOrder, frequency, retentionMonths, grossMargin, cac} | {avgTicket, visitFrequency, retentionMonths, cac} | 缺 grossMargin（后端硬编码 0.65），字段名不同 |
| 9 | ltv-education | {hourlyFee, monthlyHours, retentionMonths, extraIncomePct, cac} | {courseFee, monthsEnrolled, materialFee, examFee, cac} | 全部不匹配 |
| 10 | promotion-profit | {normalPrice, normalMargin, discount, promoOrders, normalOrders, days} | {originalPrice, discountPrice, marginRate, dailySales, incrementalRate} | 全部不匹配 |

### BUG-11：spreadsheetEngine.js 表格字段名与前端 Sheet 组件不一致

**严重程度**：严重 — 6 张表格的前后端字段名全部不一致。

| 前端 fields key | 后端 headers | 是否匹配 |
|---------------|-------------|---------|
| `activityName` | `活动名称` | 名称对应但 key 格式不同 |
| `leads` vs `带来线索` | `线索数量` | 不一致 |
| `visitors` vs `到店人数` | `到店人数` | 不一致 |
| `referralTrackingSheet` 前端 8 字段 vs 后端 8 字段 | key 和 header 完全不对应 | 不一致 |
| `marketingBudgetSheet` 前端 9 字段 vs 后端 9 字段 | key 和 header 完全不对应 | 不一致 |

### BUG-12：ReferralROI.vue — savingPct 可能为负数但显示为"节省"

**文件**：`ReferralROI.vue:90,105`  
**问题**：当转介绍 CAC 高于其他渠道 CAC 时，savingPct 为负数。第 105 行 `savingPct: parseFloat(savingPct) > 0 ? savingPct : '0'` 会隐藏负值显示为 `0`，但 savingClass 在第 106 行仍可能为 `bad`，UI 显示"节省 0%"但颜色是红色，逻辑矛盾。  
**建议**：负值时应显示"高出 X%"而非"节省 0%"。

### BUG-13：RetentionRate.vue — 沉睡客户和流失客户计算有重叠

**文件**：`RetentionRate.vue:90-92`  
**问题**：
```js
const totalCustomers = form.startCustomers + (form.newCustomers || 0)
const dormantCount = totalCustomers - form.endActive        // 所有非活跃 = 沉睡
const churnedCount = Math.max(0, form.startCustomers - form.endActive)  // 期初流失
```
- `dormantCount` 包含了 `churnedCount`（流失客户），两者不是互斥的
- UI 上展示为"活跃 / 沉睡 / 流失"三个独立状态，但沉睡和流失有重叠，总和超过总客户数

### BUG-14：ChurnRate.vue — costRatio 计算公式错误

**文件**：`ChurnRate.vue:79`  
**问题**：
```js
const costRatio = (form.avgOrder * 2).toFixed(0)
// 然后显示为 `1:${costRatio}`
```
这个公式 `客单价 × 2` 没有任何商业含义。"流失 vs 获新成本比"应该是"挽留成本 / 获新成本"，而不是客单价的两倍。

### BUG-15：ChannelCAC.vue — CAC 计算用 converted || leads 做除数

**文件**：`ChannelCAC.vue:81`  
**问题**：
```js
cac: (ch.cost / (ch.converted || ch.leads)).toFixed(2)
```
当 `converted` 为 0 时，会用 `leads` 做除数，这算出来的是"每条线索成本"而不是"获客成本"，两者含义完全不同。用户看到结果会误以为是 CAC。

### BUG-16：PromotionProfit.vue — 保本增量公式在分母为 0 时返回 NaN

**文件**：`PromotionProfit.vue:104`  
**问题**：
```js
const breakEvenIncrease = Math.ceil(form.normalOrders * (100 - form.discount) / (form.discount - (100 - form.normalMargin)))
```
当 `form.discount === (100 - form.normalMargin)` 时分母为 0，结果为 `Infinity`，`Math.ceil(Infinity)` = `Infinity`，后续第 105 行 `dailyIncrease >= Infinity` 永远为 false。

### BUG-17：conversion-funnel 漏斗后端使用 Math.max 找数组最大值

**文件**：`calculatorEngine.js:2573-2577`  
**问题**：
```js
const biggestDrop = Math.max(
    { name: '线索→体验', drop: 100 - step1 },
    { name: '体验→成交', drop: 100 - step2 },
    { name: '成交→复购', drop: 100 - step3 }
)
```
`Math.max()` 不接受对象数组，会返回 `NaN`。这是 JavaScript 语法错误。应该用 `.reduce()` 或 `Array.prototype.sort()`。

---

## 二、中等问题（7 个）

### BUG-18：CampaignROI.vue — ROI 定义不一致

**前端**（line 97）：`roi = revenue / totalCost`（营收/投入）  
**后端**（line 2482）：`roi = netProfit / investment * 100`（利润率百分比）  
同一个工具，前后端用了两种不同的 ROI 定义，如果将来前后端打通，结果会不一致。

### BUG-19：MarketingBudget.vue — 预算分配百分比加总不一定等于 100%

**文件**：`MarketingBudget.vue:96-102`  
**问题**：只对 enabled 的渠道分配权重，但 goalWeights 是基于全部 6 个渠道的百分比。当用户只启用部分渠道时，totalWeight < 100，各渠道百分比按比例放大后虽然总和为 100%，但分配比例已经不是用户选择的营销目标下的最优分配。

### BUG-20：LTVRestaurant.vue — LTV 没有乘以毛利率

**前端**（line 76）：`ltv = avgOrder * frequency * retentionMonths * (grossMargin / 100)`  
**后端**（line 2729）：`ltv = avgTicket * visitFrequency * retentionMonths`（**没有乘毛利率**）  
前端 LTV 是"毛利"，后端 LTV 是"营收"。概念不同。

### BUG-21：LTVEducation.vue — LTV 没有扣除成本

**文件**：`LTVEducation.vue:79`  
**问题**：`ltv = monthlyTotal * retentionMonths` 只计算了总收入，没有扣除教学成本（教师工资、场地租金等）。LTV 应该是"净价值"或"毛利"，不是"总营收"。

### BUG-22：所有计算器工具 category 未加入 marketingTabs

**文件**：`toolCatalog.js`  
**问题**：`douyinOperationTools` 和 `strategyCapabilityTools` 有对应的 tab 过滤，但新增的 `category: 'marketing'` 工具没有被加入任何 tab 过滤数组，在"抖音运营"等 tab 下看不到这些工具。

### BUG-23：RetentionRate.vue — period 选择器值未参与计算

**文件**：`RetentionRate.vue:6,86`  
**问题**：`form.period` 有 `30/60/90` 三个选项，但计算时只用了一次 `{{ form.period }}天` 作为标签文字。实际留存率计算没有根据选择的周期做区分，30/60/90 天的结果完全一样。

### BUG-24：ConversionFunnel.vue — 漏斗颜色在前端硬编码但后端无对应

**前端**（line 66）：`const COLORS = ['#3b82f6', '#6366f1', ...]`  
**后端**：无颜色信息  
漏斗可视化完全依赖前端，如果后端数据驱动展示，颜色信息会丢失。

---

## 三、轻微问题（4 个）

### BUG-25：UI 样式不统一 — 字体大小混用 px 和 CSS 变量

**问题**：在新增的 16 个组件中，部分样式使用 `font-size: 12px`（硬编码），部分使用 `font-size: var(--text-body-sm)`（CSS 变量）。  
**影响文件**：几乎所有新增组件，如 `CampaignROI.vue:138`、`ChannelCAC.vue:134`、`ReferralROI.vue:131` 等。

### BUG-26：UI 样式不统一 — 颜色值混用硬编码 hex 和 CSS 变量

**问题**：结果卡片中的状态颜色（#dcfce7、#fee2e2 等）全部硬编码，未使用项目的 CSS 变量系统。  
**影响范围**：所有 `good/warn/bad` 状态的颜色。

### BUG-27：toolCatalog.js — marketing-budget 的 requiredLevel 不一致

**文件**：`toolCatalog.js:170`  
**问题**：`marketing-budget` 的 `requiredLevel: MEMBER_LEVEL_STARTER`，而其他 9 个营销计算器都是 `MEMBER_LEVEL_FREE`。作为同一类别的工具，权限层级不统一。

### BUG-28：错别字/文案问题

- `ConversionFunnel.vue:26`：`⚠ 低于平均` — 建议改为"⚠️ 低于平均"（emoji 完整性）
- `ChannelCAC.vue:34`：使用了 emoji `🏆`，与项目 `no-emoji.md` 规则可能冲突（需确认此规则是否适用于用户可见的 UI 文本）

---

## 四、汇总统计

| 级别 | 数量 | 说明 |
|-----|------|------|
| 严重 | 17 | 前后端 API 字段不匹配（10 个）、表格字段不匹配（1 个）、计算逻辑错误（3 个）、JS 语法错误（1 个）、公式错误（1 个）、UI 逻辑矛盾（1 个） |
| 中等 | 7 | ROI 定义不一致、预算分配问题、LTV 概念差异、tab 缺失、周期未参与计算、颜色硬编码 |
| 轻微 | 4 | 字体/颜色不统一、权限层级不一致、文案细节 |
| **总计** | **28** | |

---

## 五、关键结论

1. **最严重的问题**：10 个计算器和 6 张表格的前后端 API 字段**完全不匹配**。虽然当前前端是纯客户端计算可以独立运行，但后端 `calculatorEngine.js` 和 `spreadsheetEngine.js` 中对应的计算器/表格逻辑完全无法被调用。如果项目架构要求前后端分离计算，这些工具将无法正常工作。

2. **公式错误**：ChurnRate 的 costRatio、RetentionRate 的沉睡/流失重叠、ConversionFunnel 后端的 Math.max 对象错误，这些是明确的 bug。

3. **概念不一致**：LTV 前端算毛利、后端算营收；ROI 前后端定义不同。同一工具的两个版本给出了不同的业务含义。

4. **UI 统一性**：新增组件的样式没有完全遵循项目现有的 CSS 变量规范，存在硬编码字体大小和颜色的情况。
